#JSGF V1.0;

/**
 * JSGF Digits Grammar file
 */

grammar digits;

public <miscellaneous> = (quit);

public <basicCmd> = <action> <object>;

<action> = (show);

<object> = (cube | sphere | quad | capsule);
